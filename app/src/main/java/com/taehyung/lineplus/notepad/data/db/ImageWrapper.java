package com.taehyung.lineplus.notepad.data.db;

import android.net.Uri;

/**
 * URI Wrapper for Image
 */
public class ImageWrapper {
    public Uri uri;

    public ImageWrapper(Uri uri) {
        this.uri = uri;
    }
}
